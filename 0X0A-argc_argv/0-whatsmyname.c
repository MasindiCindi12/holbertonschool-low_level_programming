#include "main.h"
#include <stdio.h>

/**
 * main - Print its name
 * @argc: argument count
 * @ergv: argument vector
 *
 * Return: Alwayss zero (0)
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s/n", argv[0];

        return (0);
}
